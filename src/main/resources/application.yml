spring:
  application:
    name: bank-system
  datasource:
    url: jdbc:h2:mem:user
    username: sa
    password: 1234
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console